import Card from 'components/Card';

import * as Styled from './style';

const ClientCard = ({ title, backgroundImg }) => {
  return (
    <Styled.Wrapper>
      <Card />
    </Styled.Wrapper>
  );
};

export default ClientCard;
